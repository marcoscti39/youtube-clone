import React from "react";

const LoadingSpinner = () => {
  return (
    <div className="w-[50px] h-[50px] rounded-[50%] border-t-blue-500 border-[5px] animate-spin mx-auto"></div>
  );
};

export default LoadingSpinner;
